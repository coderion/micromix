<blueprint xmlns="http://www.osgi.org/xmlns/blueprint/v1.0.0">

    <reference id="xxx" interface="org.springframework.transaction.PlatformTransactionManager"/>

    <reference id="yyy" interface="javax.persistence.EntityManagerFactory"/>

    <camelContext xmlns="http://camel.apache.org/schema/blueprint">
        <route>
            <from uri="direct:test"/>
            <to uri="jpa://fuse.pocs.blueprint.openjpa.route.Person"/>
        </route>
        <route>
            <from uri="direct:read"/>
            <pollEnrich uri="jpa://fuse.pocs.blueprint.openjpa.route.Person"/>
        </route>
    </camelContext>

</blueprint>
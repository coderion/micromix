subprojects {
    // Maven compatibility
    apply plugin: 'maven'
    group = 'com.github.micromix'
    version = '0.0-SNAPSHOT'

    repositories {
        mavenCentral()
    }
}

project('micromix-services') {
    project('micromix-services-restgateway') {}
    apply plugin: 'java'
}
